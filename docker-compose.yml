services:
  nginx:
    container_name: nginx
    image: nginx:latest
    ports:
      - "80:80/tcp"
      - "443:443/tcp"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/certs:/etc/nginx/certs
    restart: unless-stopped
    networks:
      - pihole_network

  pihole:
    container_name: pihole
    image: pihole/pihole:latest
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    environment:
      TZ: ${TZ}
      FTLCONF_webserver_api_password: ${FTLCONF_webserver_api_password}
      FTLCONF_dns_listeningMode: ${FTLCONF_dns_listeningMode}
    volumes:
      - './pihole/etc-pihole:/etc/pihole'
    cap_add:
      - SYS_NICE
    restart: unless-stopped
    networks:
      - pihole_network

#  sftpgo:
#    container_name: sftpgo
#    image: drakkan/sftpgo:latest
#    ports:
#      - "2022:2022/tcp" # SFTP
#      - "2121:2121/tcp" # FTP/FTPS
#      - "8080:8080/tcp" # Web UI (will be reverse proxied by Nginx)
#      - "50000-50100:50000-50100/tcp" # Passive FTP range
#    environment:
#      - SFTPGO_FTPD__BINDINGS__0__PORT=${SFTPGO_FTPD_PORT}
#      - SFTPGO_WEBDAV__BINDINGS__0__PORT=0 # Disable WebDAV if not needed
#    volumes:
#      - ./sftpgo/data:/srv/sftpgo
#      - ./sftpgo/home:/var/lib/sftpgo
#    restart: unless-stopped
#    networks:
#      - pihole_network

networks:
  pihole_network:
    driver: bridge
